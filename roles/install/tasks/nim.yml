- name: Download choosenim
  ansible.builtin.get_url:
    url: https://nim-lang.org/choosenim/init.sh
    dest: /tmp/choosenim.sh
    mode: +x

- name: Install choosenim
  ansible.builtin.shell:
    /tmp/choosenim.sh -y

- name: Setup Nim devel
  ansible.builtin.shell:
    choosenim devel